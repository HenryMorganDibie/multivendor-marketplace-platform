name: Sync sandbox mirror

on:
  schedule:
    - cron: "0 * * * *" # hourly
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sandbox
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure git identity
        run: |
          git config user.name "sandbox-sync-bot"
          git config user.email "actions@github.com"

      - name: Sync the platform-backend into repo root
        env:
          SRC_TOKEN: ${{ secrets.SOURCE_REPOS_TOKEN }}
        run: |
          git clone --depth 1 https://x-access-token:${SRC_TOKEN}@github.com/the platformTech/the platform-backend.git /tmp/the platform-backend
          find . -mindepth 1 -maxdepth 1 ! -name '.git' ! -name '.github' ! -name 'website' ! -name 'vendor-portal' -exec rm -rf {} +
          rsync -a --exclude='.git' --exclude='.github' /tmp/the platform-backend/ ./

      - name: Sync the platform-website into website/
        env:
          SRC_TOKEN: ${{ secrets.SOURCE_REPOS_TOKEN }}
        run: |
          git clone --depth 1 https://x-access-token:${SRC_TOKEN}@github.com/the platformTech/the platform-website.git /tmp/the platform-website
          rm -rf website
          mkdir website
          rsync -a --exclude='.git' --exclude='.github' /tmp/the platform-website/ website/

      - name: Sync the platform-vendor-portal into vendor-portal/
        env:
          SRC_TOKEN: ${{ secrets.SOURCE_REPOS_TOKEN }}
        run: |
          git clone --depth 1 https://x-access-token:${SRC_TOKEN}@github.com/the platformTech/the platform-vendor-portal.git /tmp/the platform-vendor-portal
          rm -rf vendor-portal
          mkdir vendor-portal
          rsync -a --exclude='.git' --exclude='.github' /tmp/the platform-vendor-portal/ vendor-portal/

      - name: Commit and push if anything changed
        run: |
          git add -A
          if git diff --cached --quiet; then
            echo "No changes since last sync."
          else
            git commit -m "Auto-sync from the platform-backend, the platform-website, the platform-vendor-portal"
            git push origin main
          fi
